name: Deploy to AWS Lambda

on:
  push:
    branches:
      - production
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  DOTNET_VERSION: "10.0.x"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Install AWS SAM CLI
        uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Restore dependencies
        run: dotnet restore

      - name: Build solution
        run: dotnet build KaiAssistant.sln --configuration Release --no-restore

      - name: Validate SAM template
        run: sam validate --template infra/template.yaml

      - name: SAM Build
        run: sam build --template infra/template.yaml

      - name: Create S3 bucket if not exists
        run: |
          aws s3 mb s3://kaiassistant-lambda-deployments --region ${{ env.AWS_REGION }} || true

      - name: Check and clean failed stack
        run: |
          STACK_STATUS=$(aws cloudformation describe-stacks --stack-name kaiassistant-api-stack --query 'Stacks[0].StackStatus' --output text 2>/dev/null || echo "DOES_NOT_EXIST")
          if [ "$STACK_STATUS" = "ROLLBACK_COMPLETE" ]; then
            echo "Stack in ROLLBACK_COMPLETE state, deleting..."
            aws cloudformation delete-stack --stack-name kaiassistant-api-stack
            aws cloudformation wait stack-delete-complete --stack-name kaiassistant-api-stack
            echo "Stack deleted successfully"
          fi

      - name: SAM Deploy
        run: |
          # Gmail app passwords are often shown with spaces; strip all whitespace to avoid truncation/splitting.
          SMTP_SENDER_PASSWORD_CLEAN="$(printf '%s' "${{ secrets.SMTP_SENDER_PASSWORD }}" | tr -d '[:space:]')"
          sam deploy \
            --template .aws-sam/build/template.yaml \
            --no-confirm-changeset \
            --no-fail-on-empty-changeset \
            --stack-name kaiassistant-api-stack \
            --capabilities CAPABILITY_IAM CAPABILITY_NAMED_IAM \
            --s3-bucket kaiassistant-lambda-deployments \
            --parameter-overrides \
              MongoConnectionString="${{ secrets.MONGODB_CONNECTIONSTRING }}" \
              RedisConnectionString="${{ secrets.REDIS__CONNECTIONSTRING }}" \
              MongoDatabaseName="${{ secrets.MONGODB_DATABASE }}" \
              AllowedOrigins="${{ secrets.ALLOWED_ORIGINS }}" \
              SmtpSenderEmail="${{ secrets.SMTP_SENDER_EMAIL }}" \
              SmtpReceiverEmail="${{ secrets.SMTP_RECEIVER_EMAIL }}" \
              SmtpSenderPassword="$SMTP_SENDER_PASSWORD_CLEAN" \
              SmtpEnabled="false" \
              DotNetEnvironment="Production" \
              GeminiApiKey="${{ secrets.GEMINI_API_KEY }}"

      - name: Get CloudFormation events on failure
        if: failure()
        run: |
          aws cloudformation describe-stack-events \
            --stack-name kaiassistant-api-stack \
            --max-items 20 \
            --query 'StackEvents[?ResourceStatus==`CREATE_FAILED`].[LogicalResourceId,ResourceStatusReason]' \
            --output table

      - name: Get API URL
        run: |
          API_URL=$(aws cloudformation describe-stacks \
            --stack-name kaiassistant-api-stack \
            --query 'Stacks[0].Outputs[?OutputKey==`ApiUrl`].OutputValue' \
            --output text)
          echo "API_URL=$API_URL" >> $GITHUB_ENV
          echo "🚀 API deployed to: $API_URL"
